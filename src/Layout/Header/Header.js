/** React imports */
import React from 'react';

/**
 * Header Application
 */
const Header = (props) => {

  return (
    <header>
      {props.children}
    </header>
  );
}



export default Header;